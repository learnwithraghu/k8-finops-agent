# Video 1: Why Trace the FinOps Agent

**Time Budget:** ~2 min
**Format:** Talking head / diagram (no live terminal)
**Prerequisites:** Section 07 complete — policy auditor understood; MCP + rules file familiar

---

## Key takeaways

- Section 07 prints a plain-English audit on screen — you see the answer, not the path
- An MCP + LLM agent makes many hidden steps: model calls, tool calls, retries, latency
- LangSmith records that path automatically when env vars are set — no new Python modules
- Section 09 copies the Section 07 auditor and turns on observability with `.env` only

## Demo handoff

No hands-on steps in this video. Configuration starts in `2_guide.md` — LangSmith project and `.env`.

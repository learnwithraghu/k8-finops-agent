# Video: Persistent HTTP MCP — upgrade from Section 05

**Time Budget:** 3–4 min

### Opening

<!-- TODO: write narration -->

Focus: Section 05 used Supergateway because curl cannot speak stdio MCP. Section 06 upgrades to one persistent `mcp/kubernetes` Docker container with native Streamable HTTP on port 8000, validated with `code/validate_mcp.py` — no gateway, no curl.

### Close

<!-- TODO: write narration -->

# Video: Query agent — prompt, LLM tool choice, MCP answer

**Time Budget:** 3–4 min

### Opening

<!-- TODO: write narration -->

Focus: The prompt → LLM tool choice → MCP call → plain-English answer loop in `code/query_agent.py`. Same `kubectl_get` tools over the Section 06 persistent HTTP MCP endpoint, now driven by LangChain.

### Close

<!-- TODO: write narration -->

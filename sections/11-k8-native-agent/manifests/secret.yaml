apiVersion: v1
kind: Secret
metadata:
  name: finops-agent-llm
  namespace: finops-agent
type: Opaque
stringData:
  # Replace with your real OpenAI-compatible API key.
  # NEVER commit a real key inside this manifest; create the Secret from the
  # command line or a sealed-secret/ external-secret tool instead.
  OPENAI_API_KEY: "your-api-key-here"

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8-finops-agent.fullname" . }}-tagging-rules
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "k8-finops-agent.labels" . | nindent 4 }}
data:
  tagging-rules.yaml: |
    {{- toYaml .Values.taggingRules | nindent 4 }}

# Section 04 Guide: Local Python Agent

## Teach
Build and run the first local agent that scans Kubernetes and produces a basic FinOps report.

## Use
- `agent/`: scanner, calculator, analyzer, and entrypoint code
- `config/`: pricing and tagging policy inputs

## Folder intent
- `agent/`: current runnable code moved into the section
- `config/`: the external config the agent reads

## Keep the focus
This is local-only. No Bedrock decision flow refinement and no tracker integration yet.
